import React from 'react';
import "./Settings.scss";

function Settings() {
    return (
        <section className="settings">
            Settings
        </section>
    );
};

export default Settings;